name: Open Google

on:
  push:
    branches: [ main ]

jobs:
  open-url:
    runs-on: ubuntu-latest
    steps:
      - name: sss
        run: sudo apt-get install -y xdg-utils
      - name: Open Google
        run: xdg-open https://www.google.com/
