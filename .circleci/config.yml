version: 2.1

jobs:
  build:
    macos:
      xcode: 14.2.0
    steps:
      - checkout 
      - run:
          name: Run script
          command: |
            #!/bin/bash --login -eo pipefail
            cd /
      - run: ls

workflows:
  build-only:
    jobs:
      - build
