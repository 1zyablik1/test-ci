version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.9.1
    steps:
      - run: ls
      - run:
          name: Create empty file
          command: |
            echo "hello world" > my_file.txt
            echo "Created my_file.txt"
      - store_artifacts:
          path: my_file.txt
          destination: my_artifacts
      - run: ls

  receive:
    docker:
      - image: circleci/python:3.9.1
    environment:
      MY_FILE_PATH: "${CIRCLE_WORKING_DIRECTORY}/my_artifacts/my_file.txt"
    steps:
      - run: ls
      

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - receive:
          requires:
            - build
