---
jobs:
  build:
    docker:
      - image: 1zyablik1/unity
      
    steps:
      - checkout
      - run:
          name: Build Android
          command: unity -batchmode -projectPath /unityPrj/test-ci/ -executeMethod
            BuildScript.BuildAndroid -quit
          
workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
