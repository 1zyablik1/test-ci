version: 2.1

jobs:
  build:
    docker:
      - image: circleci/android:api-30
    steps:
      - setup_remote_docker
      - checkout 
      - run: ls
      - run: pwd
      - run: ls

workflows:
  build-only:
    jobs:
      - build


