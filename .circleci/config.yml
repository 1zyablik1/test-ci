jobs:
  build:
    docker:
      - image: 1zyablik1/unity
    steps:
      - run: ls
      - checkout:
          path: /unityPrj/
      - run: unity -batchmode -projectPath /unityPrj/test-ci/ -executeMethod BuildScript.BuildAndroid -quit
      - run: ls
      
workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
