version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.9.1
    steps:
      - run:
          name: Create empty file
          command: |
            echo "hello world" > my_file.txt
            echo "Created my_file.txt"
      - store_artifacts:
          path: my_file.txt
          destination: my_artifacts
      - run: ls

  receive:
    docker:
      - image: cimg/base:stable
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - run: ls
      - run:
          name: Use environment variable
          command: |
            cat /tmp/workspace/my_artifacts/my_file.txt

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - receive:
          requires:
            - build
