version: 2.1

os:

jobs:
  build:
    macos:
      xcode: 14.2.0
    steps:
      - run: xcodebuild -version
      - checkout 
      - run: ls
      - run: pwd
      - run: cd ..
      - run: ls
      - run: cd ..
      - run: ls

workflows:
  build-only:
    jobs:
      - build


