version: 2.1

jobs:
  build:
    macos:
      xcode: 14.2.0
    steps:
      - checkout 
      - run:
          name: run
          command:  #!/bin/bash cd /
      - run: ls

workflows:
  build-only:
    jobs:
      - build


