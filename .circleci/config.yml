jobs:
  build:
    docker:
      - image: 1zyablik1/myapp
    steps:
      - run:
          name: Run tests
          command: cowsay Hi
            
workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
