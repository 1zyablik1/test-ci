version: 2.1

os:

jobs:
  build:
    macos:
      xcode: 13.3.0
    steps:
      - setup_remote_docker
      - checkout 
      - run: ls
      - run: pwd
      - run: cd ..
      - run: ls
      - run: cd ..
      - run: ls

workflows:
  build-only:
    jobs:
      - build


