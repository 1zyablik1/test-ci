version: 2.1

jobs:
  build:
    macos:
      xcode: 14.2.0
    steps:
      - checkout 
      - run: ls
      - run:
          name: Run script
          command: |
            cd /
      - run: ls
      - run: cd ..
      - run: ls

workflows:
  build-only:
    jobs:
      - build
